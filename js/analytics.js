/**
 * Page-view + event tracking is handled by the Plausible snippet loaded
 * from <head> in every page. This file is intentionally a no-op so the
 * existing <script src="js/analytics.js"> tags don't 404.
 */
